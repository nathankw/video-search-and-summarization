# SPDX-FileCopyrightText: Copyright (c) 2025 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
# http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

x-host-service: &host_service
  user: "0:0"
  network_mode: host

x-depends-on-redis-livestream-1: &depends_on_redis_livestream_1
  redis:
    condition: service_healthy
  livestream-ms-1:
    condition: service_healthy

services:
  livestream-ms-1:
    image: ${VST_LIVESTREAM_IMAGE}
    <<: *host_service
    runtime: nvidia
    container_name: livestream-ms-1
    entrypoint: ["/bin/bash", "-c", "if [ \"$$VST_INSTALL_ADDITIONAL_PACKAGES\" = \"true\" ]; then /home/vst/vst_release/tools/user_additional_install.sh; fi && exec /home/vst/vst_release/launch_vst"]
    environment:
      - DEEPSTREAM_ENABLE_SENSOR_ID_EXTRACTION=1
      - NVIDIA_VISIBLE_DEVICES=all
      - VST_INSTALL_ADDITIONAL_PACKAGES=${VST_INSTALL_ADDITIONAL_PACKAGES}
      - CONTAINER_NAME=livestream-ms-1
      - ADAPTOR=livestream-ms
      - HTTP_PORT=${LIVESTREAM_HTTP_PORT_1}
      - PROMETHEUS_PORT=${LIVESTREAM_PROMETHEUS_PORT_1}
      - CENTRALIZE_DB_NAME=${CENTRALIZE_DB_NAME}
      - CENTRALIZE_DB_USERNAME=${CENTRALIZE_DB_USERNAME}
      - CENTRALIZE_DB_PASSWORD=${CENTRALIZE_DB_PASSWORD}
      - CENTRALIZE_DB_PORT=${CENTRALIZE_DB_PORT}
      - CENTRALIZE_DB_HOSTADDR=${CENTRALIZE_DB_HOSTADDR}
      - VST_INGRESS_ENDPOINT=${VST_INGRESS_ENDPOINT}
    volumes:
      - ${VST_CONFIG_PATH}:/home/vst/vst_release/configs
      - ${VST_DATA_PATH}:/home/vst/vst_release/vst_data
      - ${VST_VIDEO_STORAGE_PATH}:/home/vst/vst_release/vst_video
      - ${VST_TEMP_FILES_PATH}:/home/vst/vst_release/webroot/temp_files
      - ${VST_CONFIG_PATH}/user_additional_install.sh:/home/vst/vst_release/tools/user_additional_install.sh
    restart: unless-stopped
    deploy:
      restart_policy:
        condition: always
    healthcheck:
      test:
        - CMD-SHELL
        - >
          bash -ec 'exec 3<>/dev/tcp/127.0.0.1/$${HTTP_PORT}; printf "GET /v1/ready HTTP/1.1\r\nHost: 127.0.0.1\r\nConnection: close\r\n\r\n" >&3; read -r status <&3; [[ "$${status}" == HTTP/*" 200 "* ]]'
      interval: 15s
      timeout: 3s
      retries: 60
      start_period: 30s
    depends_on:
      centralizedb:
        condition: service_healthy
      redis:
        condition: service_healthy

# Uncomment this to scale upto 2 livestream-ms
  # livestream-ms-2:
  #   image: ${VST_LIVESTREAM_IMAGE}
  #   user: "0:0"
  #   runtime: nvidia
  #   container_name: livestream-ms-2
  #   network_mode: host
  #   entrypoint: ["/bin/bash", "-c", "if [ \"$$VST_INSTALL_ADDITIONAL_PACKAGES\" = \"true\" ]; then /home/vst/vst_release/tools/user_additional_install.sh; fi && exec /home/vst/vst_release/launch_vst"]
  #   environment:
  #     - NVIDIA_VISIBLE_DEVICES=all
  #     - VST_INSTALL_ADDITIONAL_PACKAGES=${VST_INSTALL_ADDITIONAL_PACKAGES}
  #     - CONTAINER_NAME=livestream-ms-2
  #     - ADAPTOR=livestream-ms
  #     - HTTP_PORT=${LIVESTREAM_HTTP_PORT_2}
  #     - PROMETHEUS_PORT=${LIVESTREAM_PROMETHEUS_PORT_2}
  #     - CENTRALIZE_DB_NAME=${CENTRALIZE_DB_NAME}
  #     - CENTRALIZE_DB_USERNAME=${CENTRALIZE_DB_USERNAME}
  #     - CENTRALIZE_DB_PASSWORD=${CENTRALIZE_DB_PASSWORD}
  #     - CENTRALIZE_DB_PORT=${CENTRALIZE_DB_PORT}
  #     - CENTRALIZE_DB_HOSTADDR=${CENTRALIZE_DB_HOSTADDR}
  #   volumes:
  #     - ${VST_CONFIG_PATH}:/home/vst/vst_release/configs
  #     - ${VST_DATA_PATH}:/home/vst/vst_release/vst_data
  #     - ${VST_VIDEO_STORAGE_PATH}:/home/vst/vst_release/vst_video
  #   deploy:
  #     restart_policy:
  #       condition: always
  #   healthcheck:
  #     test:
  #       - CMD-SHELL
  #       - >
  #         bash -ec 'exec 3<>/dev/tcp/127.0.0.1/$${HTTP_PORT}; printf "GET /v1/ready HTTP/1.1\r\nHost: 127.0.0.1\r\nConnection: close\r\n\r\n" >&3; read -r status <&3; [[ "$${status}" == HTTP/*" 200 "* ]]'
  #     interval: 15s
  #     timeout: 3s
  #     retries: 60
  #     start_period: 30s
  #   depends_on:
  #     centralizedb:
  #       condition: service_healthy
  #     redis:
  #       condition: service_healthy

  sdr-http-livestream:
    image: ${SDR_IMAGE}
    <<: *host_service
    logging:
      driver: "json-file"
      options:
        max-size: "8192m"
        max-file: "3"
    container_name: sdr-http-livestream
    volumes:
      - ./sdr-config:/wdm-configs
      - /var/run/docker.sock:/var/run/docker.sock
      - ${VST_DATA_PATH}/sdr/livestream/log:/log
    restart: unless-stopped
    environment:
      PORT: 4005
      WDM_CLUSTER_CONFIG_FILE: /wdm-configs/docker_cluster_config.json
      CONTAINER_NAME: sdr-http-livestream
      WDM_MSG_KEY: ${REDIS_MSG_KEY}
      WDM_WL_REDIS_SERVER: ${REDIS_HOSTADDR}
      WDM_WL_REDIS_PORT: ${REDIS_PORT}
      WDM_WL_REDIS_MSG_FIELD: sensor.id
      WDM_WL_ADD_URL: /api/v1/live/stream/add
      WDM_WL_DELETE_URL: /api/v1/live/stream/
      WDM_WL_HEALTH_CHECK_URL: /api/v1/live/configuration
      WDM_WL_CHANGE_ID_ADD: camera_streaming
      WDM_WL_CHANGE_ID_DEL: camera_remove
      WDM_PRELOAD_WORKLOAD: ./tests/event_pre-roll.json
      WDM_CLEAR_DATA_WL: true
      WDM_KFK_ENABLE: false
      WDM_MSG_TOPIC: vst_events
      WDM_KFK_BOOTSTRAP_URL: ${KAFKA_BOOTSTRAP_URL}
      WDM_DS_SWAP_ID_NAME: false
      WDM_WL_THRESHOLD: 500
      WDM_ADD_REMOVE_RETRY_ATTEMPTS: 50
      WDM_CLUSTER_TYPE: docker
      WDM_POD_WATCH_DOCKER_DELAY: 0.5
      WDM_RESTART_DS_ON_ADD_FAIL: false
      WDM_DISABLE_WERKZEUG_LOGGING: true
      WDM_WL_OBJECT_NAME: livestream-ms
      WDM_CONSUMER_GRP_ID: sdr-livestream-cg
      WDM_CLUSTER_CONTAINER_NAMES: '["livestream-ms"]'
      VST_STREAMS_ENDPOINT: http://localhost:30000/api/v1/live/streams
      VST_STATUS_ENDPOINT: http://localhost:30000/api/v1/sensor/status
      OTEL_SDK_DISABLED: true
      WDM_INITIALIZE_FROM_VST: false
      ENVOY_REQUEST_TIMEOUT: 30
      WDM_TARGET_PORT_MAPPING: "{\"livestream-ms-1\": ${LIVESTREAM_HTTP_PORT_1}}" #Comment this to scale upto 2 livestream-ms
      #WDM_TARGET_PORT_MAPPING: "{\"livestream-ms-1\": ${LIVESTREAM_HTTP_PORT_1}, \"livestream-ms-2\": ${LIVESTREAM_HTTP_PORT_2}}" #Uncomment this to scale upto 2 livestream-ms
      OTEL_SERVICE_NAME: SDR_AGENT
      WDM_REDIS_CACHE_OBJECT: "livestream-data"
      WDM_WL_NAME_IGNORE_REGEX: ""
    deploy:
      resources:
        limits:
          memory: 300M
      restart_policy:
        condition: always
    healthcheck:
      test:
        - CMD-SHELL
        - >
          bash -ec 'exec 3<>/dev/tcp/127.0.0.1/$${PORT}; printf "GET /healthz HTTP/1.1\r\nHost: 127.0.0.1\r\nConnection: close\r\n\r\n" >&3; read -r status <&3; [[ "$${status}" == HTTP/*" 200 "* ]]'
      interval: 15s
      timeout: 3s
      retries: 60
      start_period: 5s
    depends_on:
      <<: *depends_on_redis_livestream_1
      # livestream-ms-2: #Uncomment this to scale upto 2 livestream-ms
      #   condition: service_healthy

  envoy-http-livestream:
    image: ${ENVOY_PROXY_IMAGE}
    <<: *host_service
    command: /usr/local/bin/envoy -c /etc/envoy/envoy.yaml --concurrency 16 --base-id ${LIVESTREAM_BASE_ID}
    container_name: envoy-http-livestream
    volumes:
      - ./envoy.yaml:/etc/envoy/envoy.yaml
    restart: unless-stopped
    environment:
      WDM_WL_REDIS_SERVER: ${REDIS_HOSTADDR}
      CONTAINER_NAME: envoy-http-livestream
      WDM_WL_REDIS_PORT: ${REDIS_PORT}
      WDM_KFK_BOOTSTRAP_URL: ${KAFKA_BOOTSTRAP_URL}
      WDM_WL_OBJECT_NAME: livestream-ms
      ENVOYROUTEHEADER: "streamid"
      NOHEADERTARGETCONTAINER: "livestream-01"
    depends_on:
      <<: *depends_on_redis_livestream_1
      # livestream-ms-2: #Uncomment this to scale upto 2 livestream-ms
      #   condition: service_healthy
