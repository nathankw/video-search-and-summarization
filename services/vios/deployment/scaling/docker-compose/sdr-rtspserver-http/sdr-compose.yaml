# SPDX-FileCopyrightText: Copyright (c) 2025 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
# http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

x-host-service: &host_service
  user: "0:0"
  network_mode: host

x-healthcheck-http-ready-base: &healthcheck_http_ready_base
  test:
    - CMD-SHELL
    - >
      bash -ec 'exec 3<>/dev/tcp/127.0.0.1/$${HTTP_PORT}; printf "GET /v1/ready HTTP/1.1\r\nHost: 127.0.0.1\r\nConnection: close\r\n\r\n" >&3; read -r status <&3; [[ "$${status}" == HTTP/*" 200 "* ]]'
  interval: 15s
  timeout: 3s
  retries: 60
  start_period: 30s

x-depends-on-redis-recorder-rtspservers: &depends_on_redis_recorder_rtspservers
  redis:
    condition: service_healthy
  sdr-http-recorder:
    condition: service_healthy
  rtspserver-ms-1:
    condition: service_healthy
  rtspserver-ms-2:
    condition: service_healthy
  rtspserver-ms-3:
    condition: service_healthy
  rtspserver-ms-4:
    condition: service_healthy
  rtspserver-ms-5:
    condition: service_healthy

services:
  rtspserver-ms-1:
    image: ${VST_RTSPSERVER_IMAGE}
    container_name: rtspserver-ms-1
    <<: *host_service
    runtime: nvidia
    entrypoint: ["/bin/bash", "-c", "if [ \"$$VST_INSTALL_ADDITIONAL_PACKAGES\" = \"true\" ]; then /home/vst/vst_release/tools/user_additional_install.sh; fi && exec /home/vst/vst_release/launch_vst"]
    environment:
      - VST_INSTALL_ADDITIONAL_PACKAGES=${VST_INSTALL_ADDITIONAL_PACKAGES}
      - CONTAINER_NAME=rtspserver-ms-1
      - ADAPTOR=rtsp-ms
      - HTTP_PORT=${RTSP_SERVER_HTTP_PORT_1}
      - PROMETHEUS_PORT=${RTSP_SERVER_PROMETHEUS_PORT_1}
      - RTSP_SERVER_PORT=${RTSP_SERVER_PORT_1}
      - CENTRALIZE_DB_NAME=${CENTRALIZE_DB_NAME}
      - CENTRALIZE_DB_USERNAME=${CENTRALIZE_DB_USERNAME}
      - CENTRALIZE_DB_PASSWORD=${CENTRALIZE_DB_PASSWORD}
      - CENTRALIZE_DB_PORT=${CENTRALIZE_DB_PORT}
      - CENTRALIZE_DB_HOSTADDR=${CENTRALIZE_DB_HOSTADDR}
      - STORAGE_MODULE_ENDPOINT=${STORAGE_MODULE_ENDPOINT}
      - SENSOR_MODULE_ENDPOINT=${SENSOR_MODULE_ENDPOINT}
    volumes:
      - ${VST_CONFIG_PATH}:/home/vst/vst_release/configs
      - ${VST_DATA_PATH}:/home/vst/vst_release/vst_data
      - ${VST_VIDEO_STORAGE_PATH}:/home/vst/vst_release/vst_video
      - ${VST_CONFIG_PATH}/user_additional_install.sh:/home/vst/vst_release/tools/user_additional_install.sh
    restart: unless-stopped
    deploy:
      restart_policy:
        condition: always
    healthcheck:
      <<: *healthcheck_http_ready_base
    depends_on:
      centralizedb:
        condition: service_healthy

  rtspserver-ms-2:
    image: ${VST_RTSPSERVER_IMAGE}
    container_name: rtspserver-ms-2
    <<: *host_service
    runtime: nvidia
    entrypoint: ["/bin/bash", "-c", "if [ \"$$VST_INSTALL_ADDITIONAL_PACKAGES\" = \"true\" ]; then /home/vst/vst_release/tools/user_additional_install.sh; fi && exec /home/vst/vst_release/launch_vst"]
    environment:
      - VST_INSTALL_ADDITIONAL_PACKAGES=${VST_INSTALL_ADDITIONAL_PACKAGES}
      - CONTAINER_NAME=rtspserver-ms-2
      - ADAPTOR=rtsp-ms
      - HTTP_PORT=${RTSP_SERVER_HTTP_PORT_2}
      - PROMETHEUS_PORT=${RTSP_SERVER_PROMETHEUS_PORT_2}
      - RTSP_SERVER_PORT=${RTSP_SERVER_PORT_2}
      - CENTRALIZE_DB_NAME=${CENTRALIZE_DB_NAME}
      - CENTRALIZE_DB_USERNAME=${CENTRALIZE_DB_USERNAME}
      - CENTRALIZE_DB_PASSWORD=${CENTRALIZE_DB_PASSWORD}
      - CENTRALIZE_DB_PORT=${CENTRALIZE_DB_PORT}
      - CENTRALIZE_DB_HOSTADDR=${CENTRALIZE_DB_HOSTADDR}
      - STORAGE_MODULE_ENDPOINT=${STORAGE_MODULE_ENDPOINT}
    volumes:
      - ${VST_CONFIG_PATH}:/home/vst/vst_release/configs
      - ${VST_DATA_PATH}:/home/vst/vst_release/vst_data
      - ${VST_VIDEO_STORAGE_PATH}:/home/vst/vst_release/vst_video
      - ${VST_CONFIG_PATH}/user_additional_install.sh:/home/vst/vst_release/tools/user_additional_install.sh
    restart: unless-stopped
    deploy:
      restart_policy:
        condition: always
    healthcheck:
      <<: *healthcheck_http_ready_base
    depends_on:
      centralizedb:
        condition: service_healthy

  rtspserver-ms-3:
    image: ${VST_RTSPSERVER_IMAGE}
    container_name: rtspserver-ms-3
    <<: *host_service
    runtime: nvidia
    entrypoint: ["/bin/bash", "-c", "if [ \"$$VST_INSTALL_ADDITIONAL_PACKAGES\" = \"true\" ]; then /home/vst/vst_release/tools/user_additional_install.sh; fi && exec /home/vst/vst_release/launch_vst"]
    environment:
      - VST_INSTALL_ADDITIONAL_PACKAGES=${VST_INSTALL_ADDITIONAL_PACKAGES}
      - CONTAINER_NAME=rtspserver-ms-3
      - ADAPTOR=rtsp-ms
      - HTTP_PORT=${RTSP_SERVER_HTTP_PORT_3}
      - PROMETHEUS_PORT=${RTSP_SERVER_PROMETHEUS_PORT_3}
      - RTSP_SERVER_PORT=${RTSP_SERVER_PORT_3}
      - CENTRALIZE_DB_NAME=${CENTRALIZE_DB_NAME}
      - CENTRALIZE_DB_USERNAME=${CENTRALIZE_DB_USERNAME}
      - CENTRALIZE_DB_PASSWORD=${CENTRALIZE_DB_PASSWORD}
      - CENTRALIZE_DB_PORT=${CENTRALIZE_DB_PORT}
      - CENTRALIZE_DB_HOSTADDR=${CENTRALIZE_DB_HOSTADDR}
      - STORAGE_MODULE_ENDPOINT=${STORAGE_MODULE_ENDPOINT}
    volumes:
      - ${VST_CONFIG_PATH}:/home/vst/vst_release/configs
      - ${VST_DATA_PATH}:/home/vst/vst_release/vst_data
      - ${VST_VIDEO_STORAGE_PATH}:/home/vst/vst_release/vst_video
      - ${VST_CONFIG_PATH}/user_additional_install.sh:/home/vst/vst_release/tools/user_additional_install.sh
    restart: unless-stopped
    deploy:
      restart_policy:
        condition: always
    healthcheck:
      <<: *healthcheck_http_ready_base
    depends_on:
      centralizedb:
        condition: service_healthy

  rtspserver-ms-4:
    image: ${VST_RTSPSERVER_IMAGE}
    container_name: rtspserver-ms-4
    <<: *host_service
    runtime: nvidia
    entrypoint: ["/bin/bash", "-c", "if [ \"$$VST_INSTALL_ADDITIONAL_PACKAGES\" = \"true\" ]; then /home/vst/vst_release/tools/user_additional_install.sh; fi && exec /home/vst/vst_release/launch_vst"]
    environment:
      - VST_INSTALL_ADDITIONAL_PACKAGES=${VST_INSTALL_ADDITIONAL_PACKAGES}
      - CONTAINER_NAME=rtspserver-ms-4
      - ADAPTOR=rtsp-ms
      - HTTP_PORT=${RTSP_SERVER_HTTP_PORT_4}
      - PROMETHEUS_PORT=${RTSP_SERVER_PROMETHEUS_PORT_4}
      - RTSP_SERVER_PORT=${RTSP_SERVER_PORT_4}
      - CENTRALIZE_DB_NAME=${CENTRALIZE_DB_NAME}
      - CENTRALIZE_DB_USERNAME=${CENTRALIZE_DB_USERNAME}
      - CENTRALIZE_DB_PASSWORD=${CENTRALIZE_DB_PASSWORD}
      - CENTRALIZE_DB_PORT=${CENTRALIZE_DB_PORT}
      - CENTRALIZE_DB_HOSTADDR=${CENTRALIZE_DB_HOSTADDR}
      - STORAGE_MODULE_ENDPOINT=${STORAGE_MODULE_ENDPOINT}
    volumes:
      - ${VST_CONFIG_PATH}:/home/vst/vst_release/configs
      - ${VST_DATA_PATH}:/home/vst/vst_release/vst_data
      - ${VST_VIDEO_STORAGE_PATH}:/home/vst/vst_release/vst_video
      - ${VST_CONFIG_PATH}/user_additional_install.sh:/home/vst/vst_release/tools/user_additional_install.sh
    restart: unless-stopped
    deploy:
      restart_policy:
        condition: always
    healthcheck:
      <<: *healthcheck_http_ready_base
    depends_on:
      centralizedb:
        condition: service_healthy

  rtspserver-ms-5:
    image: ${VST_RTSPSERVER_IMAGE}
    container_name: rtspserver-ms-5
    <<: *host_service
    runtime: nvidia
    entrypoint: ["/bin/bash", "-c", "if [ \"$$VST_INSTALL_ADDITIONAL_PACKAGES\" = \"true\" ]; then /home/vst/vst_release/tools/user_additional_install.sh; fi && exec /home/vst/vst_release/launch_vst"]
    environment:
      - VST_INSTALL_ADDITIONAL_PACKAGES=${VST_INSTALL_ADDITIONAL_PACKAGES}
      - CONTAINER_NAME=rtspserver-ms-5
      - ADAPTOR=rtsp-ms
      - HTTP_PORT=${RTSP_SERVER_HTTP_PORT_5}
      - PROMETHEUS_PORT=${RTSP_SERVER_PROMETHEUS_PORT_5}
      - RTSP_SERVER_PORT=${RTSP_SERVER_PORT_5}
      - CENTRALIZE_DB_NAME=${CENTRALIZE_DB_NAME}
      - CENTRALIZE_DB_USERNAME=${CENTRALIZE_DB_USERNAME}
      - CENTRALIZE_DB_PASSWORD=${CENTRALIZE_DB_PASSWORD}
      - CENTRALIZE_DB_PORT=${CENTRALIZE_DB_PORT}
      - CENTRALIZE_DB_HOSTADDR=${CENTRALIZE_DB_HOSTADDR}
      - STORAGE_MODULE_ENDPOINT=${STORAGE_MODULE_ENDPOINT}
    volumes:
      - ${VST_CONFIG_PATH}:/home/vst/vst_release/configs
      - ${VST_DATA_PATH}:/home/vst/vst_release/vst_data
      - ${VST_VIDEO_STORAGE_PATH}:/home/vst/vst_release/vst_video
      - ${VST_CONFIG_PATH}/user_additional_install.sh:/home/vst/vst_release/tools/user_additional_install.sh
    restart: unless-stopped
    deploy:
      restart_policy:
        condition: always
    healthcheck:
      <<: *healthcheck_http_ready_base
    depends_on:
      centralizedb:
        condition: service_healthy

  sdr-http-rtspserver:
    image: ${SDR_IMAGE}
    <<: *host_service
    logging:
      driver: "json-file"
      options:
        max-size: "8192m"
        max-file: "3"
    container_name: sdr-http-rtspserver
    volumes:
      - ./sdr-config:/wdm-configs
      - /var/run/docker.sock:/var/run/docker.sock
      - ${VST_DATA_PATH}/sdr/rtspserver/log:/log
    environment:
      PORT: 4003
      WDM_CLUSTER_CONFIG_FILE: /wdm-configs/docker_cluster_config.json
      CONTAINER_NAME: sdr-http-rtspserver
      WDM_MSG_KEY: ${REDIS_MSG_KEY}
      WDM_WL_REDIS_SERVER: ${REDIS_HOSTADDR}
      WDM_WL_REDIS_PORT: ${REDIS_PORT}
      WDM_WL_REDIS_MSG_FIELD: sensor.id
      WDM_WL_ADD_URL: /api/v1/proxy/stream/add
      WDM_WL_DELETE_URL: /api/v1/proxy/stream/
      WDM_WL_HEALTH_CHECK_URL: /api/v1/proxy/configuration
      WDM_WL_CHANGE_ID_ADD: camera_proxy
      WDM_WL_CHANGE_ID_DEL: camera_remove
      WDM_PRELOAD_WORKLOAD: ./tests/event_pre-roll.json
      WDM_CLEAR_DATA_WL: true
      WDM_KFK_ENABLE: false
      WDM_MSG_TOPIC: vst_events
      WDM_KFK_BOOTSTRAP_URL: ${KAFKA_BOOTSTRAP_URL}
      WDM_DS_SWAP_ID_NAME: false
      WDM_WL_THRESHOLD: 100
      WDM_ADD_REMOVE_RETRY_ATTEMPTS: 50
      WDM_CLUSTER_TYPE: docker
      WDM_POD_WATCH_DOCKER_DELAY: 0.5
      WDM_RESTART_DS_ON_ADD_FAIL: false
      WDM_DISABLE_WERKZEUG_LOGGING: true
      WDM_WL_OBJECT_NAME: rtspserver-ms
      WDM_CONSUMER_GRP_ID: sdr-rtspserver-cg
      WDM_CLUSTER_CONTAINER_NAMES: '["rtspserver-ms"]'
      VST_STREAMS_ENDPOINT: http://localhost:30000/api/v1/live/streams
      VST_STATUS_ENDPOINT: http://localhost:30000/api/v1/sensor/status
      OTEL_SDK_DISABLED: true
      WDM_INITIALIZE_FROM_VST: false
      ENVOY_REQUEST_TIMEOUT: 30
      WDM_TARGET_PORT_MAPPING: "{\"rtspserver-ms-1\": ${RTSP_SERVER_HTTP_PORT_1}, \"rtspserver-ms-2\": ${RTSP_SERVER_HTTP_PORT_2}, \"rtspserver-ms-3\": ${RTSP_SERVER_HTTP_PORT_3}, \"rtspserver-ms-4\": ${RTSP_SERVER_HTTP_PORT_4}, \"rtspserver-ms-5\": ${RTSP_SERVER_HTTP_PORT_5}}"
      OTEL_SERVICE_NAME: SDR_AGENT
      WDM_REDIS_CACHE_OBJECT: "rtspserver-data"
      WDM_WL_NAME_IGNORE_REGEX: ""
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 300M
      restart_policy:
        condition: always
    healthcheck:
      test:
        - CMD-SHELL
        - >
          bash -ec 'exec 3<>/dev/tcp/127.0.0.1/$${PORT}; printf "GET /healthz HTTP/1.1\r\nHost: 127.0.0.1\r\nConnection: close\r\n\r\n" >&3; read -r status <&3; [[ "$${status}" == HTTP/*" 200 "* ]]'
      interval: 15s
      timeout: 3s
      retries: 60
      start_period: 5s
    depends_on:
      <<: *depends_on_redis_recorder_rtspservers

  envoy-http-rtspserver:
    image: ${ENVOY_PROXY_IMAGE}
    <<: *host_service
    command: /usr/local/bin/envoy -c /etc/envoy/envoy.yaml --concurrency 16 --base-id ${RTSPSERVER_BASE_ID}
    container_name: envoy-http-rtspserver
    volumes:
      - ./envoy.yaml:/etc/envoy/envoy.yaml
    restart: unless-stopped
    environment:
      WDM_WL_REDIS_SERVER: ${REDIS_HOSTADDR}
      CONTAINER_NAME: envoy-http-rtspserver
      WDM_WL_REDIS_PORT: ${REDIS_PORT}
      WDM_KFK_BOOTSTRAP_URL: ${KAFKA_BOOTSTRAP_URL}
      WDM_WL_OBJECT_NAME: rtspserver-ms
      ENVOYROUTEHEADER: "streamid"
      NOHEADERTARGETCONTAINER: "rtspserver-01"
    depends_on:
      <<: *depends_on_redis_recorder_rtspservers